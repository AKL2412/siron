<li class="dropdown user user-menu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <img src="{{asset(app.session.get('compte').image)}}" class="user-image" alt="User Image">
              <span class="hidden-xs">
              {{app.session.get('compte').prenom|upper}}
              </span>
            </a>
            <ul class="dropdown-menu">
              <!-- User image -->
              <li class="user-header">
                <img src="{{asset(app.session.get('compte').image)}}" class="img-circle" alt="User Image">

                <p>
                  {{app.session.get('compte').prenom~" "~app.session.get('compte').nom|upper}} <br>{{app.session.get('poste')}}
                 <!--  <small>Member since Nov. 2012</small> -->
                </p>
              </li>
              <!-- Menu Body -->
              <li class="user-body">
                <!-- <div class="row">
                  <div class="col-xs-4 text-center">
                    <a href="#">Followers</a>
                  </div>
                  <div class="col-xs-4 text-center">
                    <a href="#">Sales</a>
                  </div>
                  <div class="col-xs-4 text-center">
                    <a href="#">Friends</a>
                  </div>
                </div> -->
                <!-- /.row -->
              </li>
              <!-- Menu Footer-->
              <li class="user-footer">
                <div class="pull-left">
                
                    <a href="{{url('trc_admin_mon_compte')}} " class="btn btn-default btn-flat">Profile</a>
               
                 
                  
                  
                </div>
                <div class="pull-right">
                  <a href="{{url('trc_core_logout')}} " class="btn btn-default btn-flat">Sign out</a>
                </div>
              </li>
            </ul>
          </li>